

fn generate_word() {
    
}


fn is_palindrome(word: &str) {

    let reversed_word = word.chars().rev().collect::<String>();

    if word == reversed_word {
	println!("Found a word {}", word);
    }
}



fn main() {
    is_palindrome(generate_word);
    
}
